import React from 'react';

import { OnBoardPageComponent } from '../components';

const OnboardingScreen3 = ({ navigation }: any) => {
    return (<OnBoardPageComponent screenIndex={2} onPressNext={() => { }} onPressBack={() => { }} />)
};
export default OnboardingScreen3;
